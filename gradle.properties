# Mod Properties
mod.id=simplescrolltooltips
mod.name=SimpleScrollToolTips
mod.version=1.2.0

# Shared Dependencies (used across all versions)
deps.fabric_loader=0.16.0

# Gradle JVM settings
org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8
